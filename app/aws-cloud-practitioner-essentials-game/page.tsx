export const metadata = {
  title: "AWS Cloud Practitioner Essentials Game | CleanGuy Tech",
};

import AWSGameClient from "./AWSGameClient";

export default function AWSGamePage() {
  return <AWSGameClient />;
}
